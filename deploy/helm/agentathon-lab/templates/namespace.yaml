{{- if .Values.namespace.create }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Release.Namespace }}
  labels:
    {{- include "lab.labels" . | nindent 4 }}
    agentathon/ephemeral: "true"
    agentathon/ttl-hours: {{ .Values.ttlHours | quote }}
{{- end }}

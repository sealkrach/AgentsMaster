apiVersion: v1
kind: Service
metadata:
  name: lab-runtime
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "lab.labels" . | nindent 4 }}
spec:
  selector: { app: lab-runtime }
  ports:
    - { port: {{ .Values.runtime.port }}, targetPort: {{ .Values.runtime.port }}, name: http }
